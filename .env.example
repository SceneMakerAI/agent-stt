# ── 서버 (FastAPI)
HOST=0.0.0.0
PORT=8000

# ── FFMPEG & STT —자막 호출/결과 조회
STT_HOST=127.0.0.1
STT_PORT=8001

# 자막 교정
VLLM_HOST=127.0.0.1
VLLM_PORT=8000

RDB_HOST=127.0.0.1
RDB_PORT=3306
RDB_USER=mydb
RDB_PW=mydb
RDB_NAME=mydb

# ── 다음 단계 — agent-vision
VISION_HOST=127.0.0.1
VISION_PORT=8003

# ── 디버그 — 단계별 중간 결과를 파일로 덤프 (검수용).
DEBUG_DIR=output/
